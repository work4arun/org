// Deprecated: the shared cross-director tab widget has been replaced by a
// dedicated page per director (see app/director-*/page.tsx), each using the
// generic <CategoryTabs> component for its own Role Purpose / Roles &
// Responsibilities / Expected Outcome / MRM tabs. This file is kept only
// because the sandbox this project runs in cannot delete files from the
// mounted project folder — it is unused and safe to remove manually.
export default function DirectorTabs() {
  return null;
}
